/*Create a function that takes a base number and an exponent number and 
returns the calculation.*/
function power(base,exp){
    var output = base**exp;
    return output;
};
var result = power(5,5);
console.log(result);